# local tooling / preview config — not needed for the deployed site
.claude/
.idea/

# generated by the deploy workflow (scripts/fetch-quakes.mjs), never committed
data/quakes.csv

# compact forecast artifacts are static site assets and should deploy with Pages
!data/
data/*.json
!data/forecast_model_m3.json
!data/forecast_model_m4.json
!data/forecast_model_m5.json
!data/trigger_model.json
!data/forecast_sweep.json
!data/forecast_backtest.json
_site/
node_modules/
